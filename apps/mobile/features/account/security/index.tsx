import { AppSafeArea, InterText } from "@/components/themed";
import { View } from "react-native";

export const Security = () => {
  return (
    <AppSafeArea>
      <View>
        <InterText>Security</InterText>
      </View>
    </AppSafeArea>
  );
};
