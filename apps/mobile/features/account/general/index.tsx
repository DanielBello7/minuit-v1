import { AppSafeArea, InterText } from "@/components/themed";
import { View } from "react-native";

/** General Preferences */
export const General = () => {
  return (
    <AppSafeArea>
      <View>
        <InterText>General</InterText>
      </View>
    </AppSafeArea>
  );
};
