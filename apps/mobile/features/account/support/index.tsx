import { AppSafeArea, InterText } from "@/components/themed";
import { View } from "react-native";

export const Support = () => {
  return (
    <AppSafeArea>
      <View>
        <InterText>Support</InterText>
      </View>
    </AppSafeArea>
  );
};
