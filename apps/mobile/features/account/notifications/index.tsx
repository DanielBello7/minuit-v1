import { AppSafeArea, InterText } from "@/components/themed";
import { View } from "react-native";

export const Notifications = () => {
  return (
    <AppSafeArea>
      <View>
        <InterText>Notifications</InterText>
      </View>
    </AppSafeArea>
  );
};
