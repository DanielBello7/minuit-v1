import { AppSafeArea, InterText } from "@/components/themed";
import { View } from "react-native";

/** Personal Information */
export const Personal = () => {
  return (
    <AppSafeArea>
      <View>
        <InterText>Personal</InterText>
      </View>
    </AppSafeArea>
  );
};
